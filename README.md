# ele_popup

An interactive Three.js + GSAP webpage that unfolds Ele's folded paper drawings into their full surprise drawings.

## Local Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

GitHub Pages deploys automatically from the `main` branch using the workflow in `.github/workflows/deploy-pages.yml`.
